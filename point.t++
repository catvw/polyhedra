#ifndef POINT_TXX
#define POINT_TXX

#include <cstddef>

/**
 * This may be redundant; figure it out at home.
 */
template <std::size_t D>
class point {
public:
    point();
    
    template <typename... A>
    point(const A...);
private:
    double p[D];
};

#endif /* POINT_TXX */
